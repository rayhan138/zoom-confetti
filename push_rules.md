# Git Push Rules

- **Explicit Order Only**: Do not execute any `git push` commands unless the user explicitly orders you to do so.
- **End-of-Conversation Prompt**: At the end of every conversation, ask the user: "Should I push or not?"
